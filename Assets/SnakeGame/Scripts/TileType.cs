namespace SnakeGame.Scripts
{
    /// <summary>
    /// Represents the type of tile on the game board.
    /// </summary>
    public enum TileType
    {
        Empty,
        Food,
        Snake,
        Path,
        Blocked,
        Wall,
    }
}
